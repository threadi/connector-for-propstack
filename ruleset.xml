<?xml version="1.0"?>
<ruleset name="ConnectorForPropstack" namespace="ConnectorForPropstack\ConnectorForPropstack">
	<description>Connector for Propstack Coding Standards</description>

    <!--
        Only scan files with .php extension.
    -->
    <arg name="extensions" value="php" />

    <!--
        Directories to exclude.
    -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/build/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/blocks/*</exclude-pattern>
    <exclude-pattern>*/languages/*</exclude-pattern>
    <exclude-pattern>*/svn/*</exclude-pattern>
    <exclude-pattern>*/js/*</exclude-pattern>
    <exclude-pattern>*/app/Plugin/Cache.php</exclude-pattern>
    <exclude-pattern>*/app/Dependencies/*</exclude-pattern>

    <!-- Use additional rulesets -->
	<rule ref="WordPress-Docs"/>
	<rule ref="WordPress-Extra"/>
    <rule ref="WordPress">
        <!-- Exclude direkt db query warnings -->
        <exclude name="WordPress.DB.DirectDatabaseQuery"/>
        <exclude name="WordPress.DB.PreparedSQL.NotPrepared"/>

        <!-- Excluded in favor to match PSR-4-namings of classes. -->
        <exclude name="WordPress.Files.FileName"/>

        <!-- Exclude slow query -->
        <exclude name="WordPress.DB.SlowDBQuery.slow_db_query_meta_query"/>
        <exclude name="WordPress.DB.SlowDBQuery.slow_db_query_meta_key"/>
        <exclude name="WordPress.DB.SlowDBQuery.slow_db_query_tax_query"/>

        <!-- Exclude override of globals -->
        <exclude name="WordPress.WP.GlobalVariablesOverride"/>
        <exclude name="WordPress.NamingConventions.ValidVariableName.UsedPropertyNotSnakeCase"/>
        <exclude name="WordPress.WP.I18n.NonSingularStringLiteralText" />

        <!-- prevent WCS hint for list types which are necessary for PHPStan -->
        <exclude name="Squiz.Commenting.FunctionComment.IncorrectTypeHint"/>

        <!-- Bug: https://github.com/WordPress/WordPress-Coding-Standards/issues/1865 -->
        <exclude name="WordPress.WP.CronInterval" />
    </rule>

</ruleset>
